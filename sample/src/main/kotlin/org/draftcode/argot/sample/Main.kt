package org.draftcode.argot.sample

/**
 * Entry point for the Argot sample application.
 *
 * Replaced in later milestones with a demo that dispatches to both the delegate-style and
 * annotation-style commands. For now it is a placeholder so the build is runnable.
 */
public fun main(args: Array<String>) {
    println("argot sample (placeholder) — args: ${args.joinToString(" ")}")
}
