{
  "name": "argot-site",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation and learning site for Argot",
  "type": "module",
  "scripts": {
    "dev": "ARGOT_BUILD=docs vitepress dev",
    "dev:root": "ARGOT_BUILD=root vitepress dev",
    "build:root": "ARGOT_BUILD=root vitepress build",
    "build:docs": "ARGOT_BUILD=docs vitepress build"
  },
  "devDependencies": {
    "vitepress": "1.6.4",
    "vue": "3.5.41"
  }
}
