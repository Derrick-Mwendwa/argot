package org.draftcode.argot.annotations

/**
 * Sentinel for [Option.default] meaning "no default supplied" (annotation arguments cannot be
 * `null`). Used by the processor; not intended for direct use.
 */
public const val NO_DEFAULT: String = "\u0000"
